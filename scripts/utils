#!/bin/sh

# Utility Functions
__read_property(){
	input="$@"
	temp=`cat $config_file | sed 's/\"//g' | grep -w $input | cut -d ":" -f2`
	echo ${temp##*|}
}

__wait_for_input(){
	printf 'press [ENTER] to continue...'
	read dummy
}

__wait_for_seconds(){
	local seconds="$@"
	echo "waiting ${seconds} seconds before continuing..."
	sleep ${seconds}
}

__log_error(){
	local texts="$@"
	RED='\033[0;31m'
  NC='\033[0m'
  printf "${RED}[ERROR] ${text}${NC}\n"
}

__log_warn(){
	local texts="$@"
	YELLOW='\033[0;33m'
  NC='\033[0m'
  printf "${YELLOW}[INFO] ${text}${NC}\n"
}

__log_info(){
	local texts="$@"
	GREEN='\033[0;32m'
  NC='\033[0m'
  printf "${GREEN}[WARN] ${text}${NC}\n"
}